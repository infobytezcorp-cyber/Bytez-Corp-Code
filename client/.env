# For local development — use localhost
VITE_API_URL=http://localhost:8000

# For production with ngrok (when deployed) — use ngrok tunnel for external access
# VITE_API_URL=https://scrambled-grooving-unit.ngrok-free.dev
